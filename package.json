{
  "dependencies": {
    "l13": "^0.0.22",
    "rollup": "^4.62.2",
    "vite": "^8.1.5",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "scripts": {
  "build": "rm -f index.html && cp workingIndex.html index.html && vite build && cp dist/index.html index.html && l13 build --roadroller"
  
}
}
